info:
  name: Upload PDF
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/documents
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/maximilianjandl/Downloads/Minitest5_Research1.pdf
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setVar("id", data.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
